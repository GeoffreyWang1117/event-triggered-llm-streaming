# Core dependencies
torch>=2.1.0
numpy>=1.24
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3

# LLM Integration (cloud + local backends; keys via env vars only)
openai>=1.0
httpx>=0.24
python-dotenv>=1.0

# Mock LLM server (offline reproduction of the diagnosis interface)
fastapi>=0.100
uvicorn>=0.23
pydantic>=2.0

# Configuration
pyyaml

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Utilities
tqdm
